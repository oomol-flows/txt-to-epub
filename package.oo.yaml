version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
icon: ":twemoji:blue-book:currentColor:"
name: txt-to-epub
description: txt to epub
displayName: Txt To Epub
releaseNotes: Txt To Epub
